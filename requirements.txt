# Kaggle already ships CUDA-compatible torch/torchvision, NumPy, pandas,
# Pillow, scikit-learn, matplotlib, and OpenCV. Do not replace its torch wheel:
# it can remove P100 (sm_60) support. ``pytorch-msssim`` is optional; losses.py
# has a native SSIM fallback so an offline Kaggle notebook still runs end-to-end.
numpy>=1.24
pandas>=1.5
Pillow>=9.0
scikit-learn>=1.2
matplotlib>=3.7
opencv-python-headless>=4.8
